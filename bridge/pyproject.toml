[project]
name = "google-harness-bridge-server"
version = "0.1.0"
description = "HTTP bridge for Google's native agent runtime (localharness)"
requires-python = ">=3.10"
dependencies = [
    "google-antigravity",
    "websockets",
    "protobuf",
]
